<h1><%= @house.name %><%= link_to "(edit)", edit_house_path(house) %></h1>

<% @house.students.each do |student| %>
  <div class="student">
    <h2><%= student.name %></h2>
    <%= image_tag student.img_url %>
  </div>
<% end %>
